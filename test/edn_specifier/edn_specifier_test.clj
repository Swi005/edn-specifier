(ns edn-specifier.edn-specifier-test
  (:require [clojure.test :refer [deftest is testing]]
            [edn-specifier.edn-specifier :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
